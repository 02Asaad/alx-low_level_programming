#include "main.h"
#include <stdio.h>
#include <ctype.h>

/**
 * main - check for codes
 *
 * Return: As alwayss, 0.
 */
int _isupper(int c);
{
	int c = A, a;

	if (c == 'A')
	{
	printf("%c", _isupper(int c));
	return (1);
	}
	else 
	return (0);
}
