#include <stdio.h>

/**
 * main - Program to print single digit numbers in
 * base 16, follow by a new line.
 *
 * Return: return to 0.
 */

int main(void)
{
	int i;

	for (i = 0; i < 16; i++)
	putchar(i);
	putchar("\n");
	return (0);
}
