#include <stdio.h>

/**
 * main - Program to print single digit numbers in
 * base 10, follow by a new line.
 *
 * Return: return to 0.
 */

int main(void)
{
	int ch;

	for (ch = 0; ch < 10; ch++)
	putchar(ch);
	putchar('\n');
	return (0);
}
